# Dependencies
node_modules/

# Build output
dist/

# Astro
.astro/

# Environment variables
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local Netlify folder
.netlify/

# Auto-generated YouTube thumbnail caches.
# Both paths are regenerated by `npm run download-heroes` (prebuild hook,
# which mirrors a single download into both directories) and persisted
# across Netlify builds via netlify-plugin-cache. Don't commit these.
src/assets/hero-cache/*.jpg
src/assets/images/hero-cache/*.jpg

# YouTube OAuth credentials & tokens
credentials/
youtube_tokens.json
client_secret*.json
descriptions_pushed.json

# Descriptions (generated, not source-controlled)
descriptions/

# Tag audit working files
tag-audit.csv
tag-rename*.json
# Cloudflare Workers
comments-api/node_modules/
comments-api/.wrangler/
comments-api/.dev.vars

# Local notes — not published
voice.md


# Generated by playlist tooling (scripts/dump-uploads.cjs, match-plan.cjs, create-playlists.cjs)
scripts/.uploads.json
scripts/.playlist-resolved.json
scripts/.playlists-created.json
scripts/.descriptions-updated.json
tag-matrix.csv
tag-summary.csv

# Claude Code local tooling — session commands and machine-local settings.
# The commands were tracked by accident (a stray `git add -A` on 07-28) and
# untracked on 07-31. Other .claude/ paths stay shareable if ever wanted.
.claude/commands/
.claude/settings.local.json

# local YouTube description pass logs
scripts/.description-pass.log
scripts/.description-pass.launchd.log
