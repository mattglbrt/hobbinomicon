---
/**
 * Dark Mode Toggle Component
 * Toggles between light and dark themes with localStorage persistence
 */
---

<button
  class="dark-mode-toggle icon-btn p-2.5 rounded-xl"
  aria-label="Toggle dark mode"
  title="Toggle dark mode"
>
  <!-- Sun icon (shown in dark mode) -->
  <svg
    class="sun-icon w-4 h-4 text-white/80 hidden"
    fill="none"
    stroke="currentColor"
    viewBox="0 0 24 24"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"
    />
  </svg>
  <!-- Moon icon (shown in light mode) -->
  <svg
    class="moon-icon w-4 h-4 text-white/80"
    fill="none"
    stroke="currentColor"
    viewBox="0 0 24 24"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"
    />
  </svg>
</button>

<script is:inline>
  (function() {
    function updateAllIcons(isDark) {
      // Query icons fresh each time to handle cloned elements
      document.querySelectorAll('.sun-icon').forEach(function(icon) {
        if (isDark) {
          icon.classList.remove('hidden');
        } else {
          icon.classList.add('hidden');
        }
      });
      document.querySelectorAll('.moon-icon').forEach(function(icon) {
        if (isDark) {
          icon.classList.add('hidden');
        } else {
          icon.classList.remove('hidden');
        }
      });
    }

    function toggleDarkMode() {
      var html = document.documentElement;
      var isDark = html.classList.toggle('dark');
      localStorage.setItem('darkMode', isDark ? 'dark' : 'light');
      updateAllIcons(isDark);
    }

    function initDarkModeToggle() {
      // Set initial icon state
      var isDark = document.documentElement.classList.contains('dark');
      updateAllIcons(isDark);

      // Add click handler to all toggles
      document.querySelectorAll('.dark-mode-toggle').forEach(function(toggle) {
        // Skip if already initialized
        if (toggle.dataset.initialized) return;
        toggle.dataset.initialized = 'true';
        toggle.addEventListener('click', toggleDarkMode);
      });
    }

    // Initialize when DOM is ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initDarkModeToggle);
    } else {
      initDarkModeToggle();
    }

    // Re-initialize after View Transitions navigation
    document.addEventListener('astro:after-swap', initDarkModeToggle);
  })();
</script>
