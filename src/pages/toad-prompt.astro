<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>??? - The Hobbinomicon</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: #000000;
      color: #4ade80;
      font-family: 'Courier New', monospace;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .container {
      max-width: 48rem;
      width: 100%;
      position: relative;
      z-index: 10;
    }

    .terminal-border {
      border: 2px solid #4ade80;
      padding: 0.25rem;
    }

    .terminal-inner {
      border: 1px solid #4ade80;
      background-color: #000000;
      padding: 2rem;
    }

    .header-box {
      border: 2px solid #4ade80;
      padding: 1rem;
      margin-bottom: 2rem;
      position: relative;
    }

    .header-box::before {
      content: '';
      position: absolute;
      top: 4px;
      left: 4px;
      right: 4px;
      bottom: 4px;
      border: 1px solid #4ade80;
      pointer-events: none;
    }

    .header-title {
      text-align: center;
      font-size: 1rem;
      font-weight: bold;
      margin-bottom: 0.5rem;
    }

    .header-copyright {
      text-align: center;
      font-size: 0.875rem;
    }

    @media (min-width: 768px) {
      .header-title {
        font-size: 1.25rem;
      }

      .header-copyright {
        font-size: 1rem;
      }
    }

    .content {
      margin-bottom: 1.5rem;
    }

    .prompt-box {
      border: 1px solid #4ade80;
      padding: 1.5rem;
      margin: 2rem 0;
      text-align: center;
    }

    .warning {
      font-size: 1.5rem;
      margin-bottom: 0.5rem;
      animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    .main-question {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 1rem;
    }

    .caution {
      font-size: 0.875rem;
      opacity: 0.7;
    }

    .buttons {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      margin-top: 2rem;
    }

    @media (min-width: 768px) {
      .buttons {
        flex-direction: row;
        justify-content: center;
        align-items: center;
      }

      .header-text {
        font-size: 0.875rem;
      }

      .warning {
        font-size: 1.875rem;
      }

      .main-question {
        font-size: 1.875rem;
      }
    }

    .button {
      border: 2px solid #4ade80;
      padding: 1rem 2rem;
      background-color: transparent;
      color: #4ade80;
      cursor: pointer;
      text-align: center;
      text-decoration: none;
      display: block;
      transition: all 0.3s;
      width: 100%;
    }

    @media (min-width: 768px) {
      .button {
        width: auto;
      }
    }

    .button:hover {
      background-color: #4ade80;
      color: #000000;
      box-shadow: 0 0 10px rgba(74, 222, 128, 0.5);
    }

    .button-title {
      font-size: 1.5rem;
      font-weight: bold;
    }

    @media (min-width: 768px) {
      .button-title {
        font-size: 1.875rem;
      }
    }

    .button-subtitle {
      display: block;
      font-size: 0.75rem;
      margin-top: 0.25rem;
      opacity: 0.7;
    }

    .button:hover .button-subtitle {
      opacity: 1;
    }

    .cursor-line {
      margin-top: 2rem;
      font-size: 0.875rem;
    }

    .cursor {
      animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite;
    }

    @keyframes ping {
      75%, 100% {
        opacity: 0;
      }
    }

    .footer {
      margin-top: 3rem;
      padding-top: 1.5rem;
      border-top: 1px solid rgba(74, 222, 128, 0.3);
      text-align: center;
      font-size: 0.75rem;
      opacity: 0.5;
    }

    .scanlines {
      pointer-events: none;
      position: fixed;
      inset: 0;
      opacity: 0.1;
      z-index: 0;
      background: repeating-linear-gradient(
        0deg,
        rgba(0, 255, 0, 0.1) 0px,
        transparent 1px,
        transparent 2px,
        rgba(0, 255, 0, 0.1) 3px
      );
      animation: flicker 0.15s infinite;
    }

    @keyframes flicker {
      0%, 100% { opacity: 0.1; }
      50% { opacity: 0.095; }
    }

    .text-shadow {
      text-shadow: 0 0 5px rgba(74, 222, 128, 0.5);
    }
  </style>
</head>
<body>
  <div class="scanlines"></div>

  <div class="container">
    <div class="terminal-border">
      <div class="terminal-inner text-shadow">
        <!-- Terminal Header -->
        <div class="header-box">
          <div class="header-title">HOBBINOMICON SYSTEM v1.0</div>
          <div class="header-copyright">Copyright (C) 1990</div>
        </div>

    

        <!-- Main Prompt -->
        <div class="prompt-box">
       
          <p class="main-question">DO YOU WISH TO LICK THE TOAD?</p>
          <p class="warning">[CAUTION: EFFECTS MAY VARY]</p>
        </div>

        <!-- Options -->
        <div class="buttons">
          <a href="/secret-toad" class="button">
            <span class="button-title">&gt; [YES]</span>
            <span class="button-subtitle">INITIATE SEQUENCE</span>
          </a>

          <a href="/" class="button">
            <span class="button-title">&gt; [NO]</span>
            <span class="button-subtitle">RETURN TO SAFETY</span>
          </a>
        </div>

        <!-- Blinking Cursor -->
        <div class="cursor-line">
          <p>&gt; AWAITING INPUT<span class="cursor">_</span></p>
        </div>

        <!-- Terminal Footer -->
        <pre class="footer">
SYSTEM MEMORY: 666K  |  FREE SPACE: ADEQUATE  |  TOAD STATUS: PRESENT
        </pre>
      </div>
    </div>
  </div>
</body>
</html>
