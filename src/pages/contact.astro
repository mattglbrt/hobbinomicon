---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout
  title="Contact - The Hobbinomicon"
  description="Get in touch with The Hobbinomicon. Send feedback, suggestions, or questions about miniature painting, wargaming, and hobby content."
>
  <Header theme="light" />

  <div class="min-h-screen bg-parchment">
    <!-- Hero Section -->
    <section class="relative bg-ink text-white py-24 md:py-32 overflow-hidden">
      <div class="absolute inset-0 opacity-10">
        <div class="absolute inset-0 bg-gradient-to-br from-parchment/20 to-transparent"></div>
      </div>

      <div class="max-w-4xl mx-auto px-6 relative z-10">
        <a href="/" class="inline-block text-white/80 hover:text-white mb-6 transition-colors">
          ← Back to Home
        </a>
        <h1 class="text-5xl md:text-6xl lg:text-7xl font-heading font-bold mb-6">
          Get in Touch
        </h1>
        <p class="text-2xl md:text-3xl text-white/90 italic font-serif leading-relaxed">
          Questions, suggestions, or just want to say hello?
        </p>
      </div>
    </section>

    <!-- Main Content -->
    <article class="max-w-3xl mx-auto px-6 py-16 md:py-24">
      <!-- Introduction -->
      <div class="prose prose-lg max-w-none mb-12">
        <p class="text-xl text-ink leading-relaxed mb-8">
          I'd love to hear from you! Whether you have feedback on a tutorial, a resource to suggest,
          questions about a technique, or just want to connect with a fellow hobbyist, drop me a message below.
        </p>
      </div>

      <!-- Contact Form -->
      <div class="bg-white/50 backdrop-blur-sm p-8 md:p-12 rounded-lg border-2 border-ink/10">
        <form
          name="contact"
          method="POST"
          data-netlify="true"
          netlify-honeypot="bot-field"
          action="/contact-success"
          class="space-y-6"
        >
          <!-- Hidden fields for Netlify -->
          <input type="hidden" name="form-name" value="contact" />
          <p class="hidden">
            <label>
              Don't fill this out if you're human: <input name="bot-field" />
            </label>
          </p>

          <!-- Name Field -->
          <div>
            <label for="name" class="block text-lg font-heading font-bold text-ink mb-2">
              Name <span class="text-red-600">*</span>
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-3 bg-white border-2 border-ink/20 rounded-lg focus:border-ink focus:outline-none focus:ring-2 focus:ring-ink/20 transition-colors text-ink"
              placeholder="Your name"
            />
          </div>

          <!-- Email Field -->
          <div>
            <label for="email" class="block text-lg font-heading font-bold text-ink mb-2">
              Email <span class="text-red-600">*</span>
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 bg-white border-2 border-ink/20 rounded-lg focus:border-ink focus:outline-none focus:ring-2 focus:ring-ink/20 transition-colors text-ink"
              placeholder="your.email@example.com"
            />
          </div>

          <!-- Subject Field -->
          <div>
            <label for="subject" class="block text-lg font-heading font-bold text-ink mb-2">
              Subject <span class="text-red-600">*</span>
            </label>
            <input
              type="text"
              id="subject"
              name="subject"
              required
              class="w-full px-4 py-3 bg-white border-2 border-ink/20 rounded-lg focus:border-ink focus:outline-none focus:ring-2 focus:ring-ink/20 transition-colors text-ink"
              placeholder="What's this about?"
            />
          </div>

          <!-- Message Field -->
          <div>
            <label for="message" class="block text-lg font-heading font-bold text-ink mb-2">
              Message <span class="text-red-600">*</span>
            </label>
            <textarea
              id="message"
              name="message"
              required
              rows="6"
              class="w-full px-4 py-3 bg-white border-2 border-ink/20 rounded-lg focus:border-ink focus:outline-none focus:ring-2 focus:ring-ink/20 transition-colors text-ink resize-y"
              placeholder="Your message..."
            ></textarea>
          </div>

          <!-- Submit Button -->
          <div class="pt-4">
            <button
              type="submit"
              class="w-full md:w-auto px-8 py-4 bg-ink text-white font-heading font-bold rounded-lg hover:bg-ink/90 transition-colors focus:outline-none focus:ring-4 focus:ring-ink/20"
            >
              Send Message
            </button>
          </div>
        </form>
      </div>

      <!-- Additional Info -->
      <div class="mt-12 text-center">
        <p class="text-ink/60 italic">
          I'll get back to you as soon as possible. Thanks for reaching out!
        </p>
      </div>
    </article>

    <Footer />
  </div>
</BaseLayout>
